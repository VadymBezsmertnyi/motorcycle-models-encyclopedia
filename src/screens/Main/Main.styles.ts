import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { flex: 1 },
  imageBackground: {
    position: "absolute",
    width: "100%",
    height: "100%",
    opacity: 0.2,
  },
  containerList: {
    padding: 15,
    paddingTop: 20,
    gap: 10,
  },
});
